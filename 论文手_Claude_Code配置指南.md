# 论文手 Claude Code 配置指南

> 适配角色：数学建模 **论文手** — 负责论文撰写、格式排版、图表整合、查重修正、最终成品
> 你的工具链：LaTeX / Word + 文献管理 + 图表工具 + 查重工具

---

## 一、Claude Code 能帮你做什么

| 场景              | Claude Code 能力                           |
| ----------------- | ------------------------------------------ |
| 论文结构设计      | 根据题目类型生成论文大纲和章节规划         |
| 模型描述撰写      | 把建模手的公式和思路转化为规范的论文语言   |
| 图表说明撰写      | 根据图表自动生成符合学术规范的文字描述     |
| LaTeX 排版        | 生成/修改 LaTeX 模板，格式化表格，调整版面 |
| 摘要打磨          | 多轮润色中英文摘要，确保控制在字数限制内   |
| 查重预检          | 标记可能重复的段落，建议改写               |
| 格式一致性检查    | 全文检查标题、编号、引用、图表格式是否统一 |
| 4天实战 Day4 冲刺 | 在最短时间内完成全文缝合和排版             |

---

## 二、推荐安装的插件/Skills

### 必装（核心生产力）

**1. Superpowers 插件包**

```bash
claude plugins install superpowers
```

你高频使用的子技能：

- `brainstorming` — 论文叙事逻辑设计：如何讲好一个"问题 → 模型 → 求解 → 结论"的故事
- `verification-before-completion` — **你最重要的技能**。提交论文前强制逐项检查：摘要/关键词/章节编号/图表编号/参考文献格式/附录完整性
- `writing-plans` — 论文分章节规划，每部分分配时间和字数预算
- `receiving-code-review` — 把论文当成"代码"来审查：逻辑连贯性、格式一致性、表达准确性

**2. Planning with Files 插件**

```bash
claude plugins install planning-with-files
```

针对论文写作的结构化管理：

- `task_plan.md` — 论文章节日程（Day1 写哪几节，Day2 写哪几节…）
- `findings.md` — 收集的写作素材（优秀论文的表达方式、常用句式、图表描述模板）
- `progress.md` — 追踪每节的完成状态和字数

**3. Claude HUD 插件**

```bash
claude plugins install claude-hud
```

写作需要长时间沉浸在上下文里，HUD 让你随时知道 token 余量，避免写到一半上下文溢出。

### 可选（按需）

**4. Deep Research（内置 slash command）**

```
/deep-research "数学建模国赛优秀论文的写作特点和结构分析"
```

调研往届优秀论文的写作套路、常用表达、图表规范。

---

## 三、settings.json 配置

在项目目录 `D:\虚拟C盘\数学建模培训\.claude\settings.local.json` 中配置（可与队友共用）：

```json
{
  "permissions": {
    "allow": [
      "Bash(pdflatex *)",
      "Bash(xelatex *)",
      "Bash(latexmk *)",
      "Bash(bibtex *)",
      "Bash(pdftotext *)",
      "Bash(python *)",
      "Bash(python3 *)",
      "Bash(ls *)",
      "Bash(cd *)",
      "Bash(cat *)",
      "Bash(mkdir *)",
      "Bash(cp *)",
      "Bash(mv *)",
      "Bash(code *)",
      "Bash(git *)",
      "WebFetch(*)",
      "WebSearch(*)"
    ],
    "deny": []
  },
  "enableAllProjectMcpServers": true
}
```

> **说明**：
>
> - `pdflatex` / `xelatex` / `latexmk` — 编译 LaTeX 论文，**必须开启**
> - `bibtex` — 参考文献管理
> - `pdftotext` — 提取参考论文文本（用于学习表达，不是抄袭）
> - `WebFetch` — 查阅学术写作规范、LaTeX 宏包文档

---

## 四、推荐 Hook 配置

自动编译 LaTeX 并检查 error：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\\\\.tex$'; then cd D:/虚拟C盘/数学建模培训/paper && latexmk -pdf -interaction=nonstopmode main.tex 2>&1 | tail -5; fi"
          }
        ]
      }
    ]
  }
}
```

> 每次修改 `.tex` 文件后自动编译，有错立即发现。

---

## 五、项目 CLAUDE.md 配置

在项目根目录创建（追加你的论文写作专属部分）：

```markdown
# 数学建模国赛 — 论文手工作区

## 我的角色
负责竞赛论文的撰写、排版、图表整合和最终提交。我是全队成果的"包装者"——好的论文才能让好的模型被评委看到。

## 论文结构（国赛标准）
1. 摘要（最重要！评委第一印象）
2. 问题重述
3. 模型假设与符号说明
4. 模型建立与求解（核心章节）
5. 模型验证与分析
6. 模型评价与改进
7. 参考文献
8. 附录（代码、数据）

## 写作原则
- 摘要是门面，比赛最后 6 小时必须反复打磨
- 图表要有自明性（脱离正文也能看懂）
- 公式在正文中要有解释，不能只堆公式
- 结果分析要有"为什么"，不只是"是什么"
- 语言规范但不过度学术化，让评委读得顺畅

## 常用指令
- 写论文前先 brainstorm 章节叙事逻辑
- 每写完一章检查格式一致性
- 最后提交前运行 verification-before-completion 做全面检查
- 查重段落标注后手动改写（不用 AI 替换，会不自然）
```

---

## 六、快速上手流程

### 第一步：安装插件

```bash
claude plugins install superpowers
claude plugins install planning-with-files
claude plugins install claude-hud
```

### 第二步：准备 LaTeX 环境

确认系统已安装 TeX Live 或 MiKTeX：

```bash
pdflatex --version
```

### 第三步：初始化论文模板

在项目目录下创建 `paper/` 文件夹，放入 LaTeX 模板。让 Claude 帮你生成初始模板：

```
"帮我生成一个数学建模国赛的 LaTeX 论文模板，包含：
 - 封面页
 - 标准章节结构
 - 表格和图片的示例代码
 - 参考文献格式（GB/T 7714）
 - 附录章节"
```

### 第四步：日常使用模式

```
# 写摘要
"以下是我们的模型和主要结果，帮我写一版 500 字以内的摘要，要求突出创新点和量化结果"

# 润色段落
"这段模型描述太技术化了，帮我改得更清晰易懂，同时保持学术规范"

# 生成图表说明
"根据这张对比图，帮我写一段图表说明，格式为'图X展示了...从图中可以看出...'"

# 格式检查
"帮我全文检查：
 - 章节编号是否连续
 - 图表编号是否与引用一致
 - 参考文献格式是否统一
 - 是否存在'如图X所示'但图还没插入的情况"

# 提交前全面检查
/plan → 创建论文提交检查清单 → brainstrom 逐项过 → verification-before-completion
```

---

## 七、4 天实战节奏 × Claude Code 使用策略

| 天数           | 你的任务                                   | Claude Code 用法                                       |
| -------------- | ------------------------------------------ | ------------------------------------------------------ |
| **Day1** | 参与审题 + 开始写背景/问题重述             | brainstorm 论文叙事结构；生成初始章节框架              |
| **Day2** | 跟进模型，写模型描述 + 符号说明            | 把建模手的公式转成论文语言；生成符号表                 |
| **Day3** | 跟进结果，写结果分析 + 图表说明            | 批量生成图表描述；写结果讨论段落                       |
| **Day4** | **冲刺日** — 全文缝合 + 排版 + 查重 | 全文润色；LaTeX 编译调试；查重段落改写；格式一致性检查 |

> ⚠️ **关键策略**：不要等 Day4 才开始写！前三天每天产出对应章节，Day4 只做缝合和打磨。Claude Code 帮你加速每个阶段。

---

## 八、国赛 72 小时的 Claude Code 策略

| 时间段     | 重点                      | 用法                                      |
| ---------- | ------------------------- | ----------------------------------------- |
| 前 8 小时  | 选题 + 论文框架           | 确定题目后立即生成论文骨架和 LaTeX 模板   |
| 8-24 小时  | 背景 + 问题重述 + 假设    | 撰写前两章，模型假设同步建模手            |
| 24-48 小时 | 模型描述（核心章）        | 跟随编程手/建模手的进度，持续更新模型章节 |
| 48-60 小时 | 结果分析 + 图表说明       | 批量生成图表；写结果分析和讨论            |
| 60-68 小时 | **摘要 + 全文打磨** | 摘要反复修改（最重要！）；全文润色        |
| 68-72 小时 | 排版 + 查重 + 提交        | 最终格式检查；查重修正；生成 PDF 提交版本 |

---

## 九、论文写作常用 Prompt 模板

### 摘要模板

```
"题目：{题目}
主要模型：{模型列表}
主要结果：{量化结果}
创新点：{创新点}
请帮我写一版 500 字以内的摘要，三段式结构：
第一段：问题背景和本文工作
第二段：采用的模型和方法
第三段：主要结果和结论"
```

### 模型描述模板

```
"请把以下公式推导改写为规范的论文段落，要求：
 - 先解释符号含义
 - 再说明推导思路
 - 最后给出结论公式
 - 避免直接从公式到公式，中间要有文字解释

公式推导：
{粘贴建模手给的推导}"
```

### 结果分析模板

```
"根据以下数据/图表，帮我写一段结果分析，包含：
 1. 数据整体趋势描述
 2. 关键数值的解读（为什么是这个值）
 3. 与预期的对比
 4. 结果的实际意义

数据：
{粘贴数据}"
```

---

> 📌 **一句话**：论文是评委看到的第一样也是最后一样东西 — Claude Code 帮你写得快、排得好、查得全，但论文的叙事逻辑、创新亮点、语言温度，由你掌控。

