# 建模手 Claude Code 配置指南

> 适配角色：数学建模 **建模手** — 负责问题分析、模型设计、公式推导、方法选型、模型文档
> 你的工具链：数学推导 + 文献调研 + 模型文档 + LaTeX 公式

---

## 一、Claude Code 能帮你做什么

| 场景 | Claude Code 能力 |
|------|------------------|
| 拿到题目，分析问题类型 | 多角度拆解问题，对比可能的建模路径 |
| 调研某类模型的适用条件 | 深度搜索 + 对比分析（deep-research） |
| 设计整体建模框架 | 头脑风暴 → 结构化方案输出 |
| 公式推导与验证 | 推导过程检查、边界条件验证 |
| 比较多种模型优劣 | 生成对比表（适用条件、优缺点、复杂度） |
| 整理模型文档给论文手 | 自动生成模型描述、符号说明、流程图 |
| 理解论文中的数学方法 | 上传 PDF → Claude 解读方法 → 你判断是否可用 |

---

## 二、推荐安装的插件/Skills

### 必装（核心生产力）

**1. Superpowers 插件包**

```bash
claude plugins install superpowers
```

你高频使用的子技能：
- `brainstorming` — **你最核心的技能**。拿到题目后，和 Claude 深度讨论建模思路、列出所有可行方案、分析利弊，再确定最终框架
- `writing-plans` — 把建模方案结构化，输出清晰的执行计划给编程手
- `verification-before-completion` — 模型设计完成后，检查假设是否合理、边界是否覆盖
- `receiving-code-review` — 编程手实现后，你拿代码对照你的模型设计做审查

**2. Deep Research（内置 slash command）**

无需额外安装，直接使用 `/deep-research`。

当你需要：
- 调研某种模型（如"灰色预测模型在环境领域的应用"）
- 对比多种评价方法（如"AHP vs TOPSIS vs 熵权法 适用场景"）
- 查找某类题型的历史解法（如"国赛优化类题目的常见建模套路"）

Claude 会自动进行多源搜索 → 交叉验证 → 输出带引用的研究报告。

**3. Planning with Files 插件**

```bash
claude plugins install planning-with-files
```

每个专题训练开始前，用 `plan` 命令：
- `task_plan.md` — 你的建模任务拆解（问题分析 → 假设 → 模型选型 → 公式推导 → 验证）
- `findings.md` — 调研过程中的关键发现（某模型的适用条件、参考论文的方法）
- `progress.md` — 追踪进度（Day1 框架 / Day2 推导 / Day3 验证 / Day4 配合论文）

### 可选（按需）

**4. Claude HUD 插件**

```bash
claude plugins install claude-hud
```

状态栏常驻，适合长时间调研/推导时随时感知上下文消耗。

---

## 三、settings.json 配置

在项目目录 `D:\虚拟C盘\数学建模培训\.claude\settings.local.json` 中配置（可与编程手共用同一份，或各自维护）：

```json
{
  "permissions": {
    "allow": [
      "Bash(pdftotext *)",
      "Bash(python *)",
      "Bash(python3 *)",
      "Bash(ls *)",
      "Bash(cd *)",
      "Bash(cat *)",
      "Bash(mkdir *)",
      "Bash(cp *)",
      "Bash(mv *)",
      "Bash(code *)",
      "WebSearch(*)",
      "WebFetch(*)"
    ],
    "deny": []
  },
  "enableAllProjectMcpServers": true
}
```

> **说明**：
> - `WebSearch` 和 `WebFetch` 是建模调研的核心权限，**必须开启**，否则 deep-research 无法工作
> - `pdftotext` 用于提取论文和教材 PDF
> - `python` 用于验证简单公式计算

---

## 四、项目 CLAUDE.md 配置

在项目根目录创建（如果队长已经创建，在这些内容上追加你的建模专属部分）：

```markdown
# 数学建模国赛 — 建模手工作区

## 我的角色
负责问题分析、模型设计、公式推导、方法选型。为编程手提供清晰的模型规格，为论文手提供准确的模型描述。

## 建模方法论
- 拿到题目先做问题分解（自顶向下）
- 列出所有可能的建模路径，标注适用条件和限制
- 对每个候选模型做代价/收益评估（复杂度 vs 精度 vs 可解释性）
- 确定方案后输出结构化文档给编程手

## 常用工作流
- 调研：/deep-research "某模型在某领域的应用"
- 设计：brainstorming → 确定框架 → writing-plans
- 验证：模型假设检验 → 边界条件推导 → verification-before-completion
- 审查：编程手实现后 → receiving-code-review 对照模型设计审查

## 模型文档模板（每次训练必输出）
1. 问题重述与假设
2. 符号说明表
3. 模型选择理由（对比表）
4. 模型详细推导
5. 模型验证方法
6. 优缺点与改进方向
```

---

## 五、快速上手流程

### 第一步：安装插件

```bash
claude plugins install superpowers
claude plugins install planning-with-files
claude plugins install claude-hud
```

### 第二步：验证网络权限

在 Claude Code 中测试：
```
/search 数学建模 预测模型 适用条件
```

确认能正常返回搜索结果。

### 第三步：配置项目文件

把上方的 settings.local.json 和 CLAUDE.md 放到对应位置。

### 第四步：日常使用模式

```
# 调研
/deep-research "时间序列预测方法在环境监测数据中的应用和对比"

# 头脑风暴（最重要）
"我们拿到了一个关于城市空气质量评价的题目，帮我头脑风暴可能的建模路径：
 - 需要考虑什么因素？
 - 有哪些候选的评价模型？
 - 各自的优缺点是什么？"

# 输出建模方案
"基于刚才的讨论，帮我整理一份结构化的建模方案，包括：
 1. 问题分解 2. 模型假设 3. 模型框架 4. 符号说明 5. 输入输出"

# 验证模型设计
"帮我检查这个模型的假设是否合理，有没有遗漏的边界条件"

# 审查编程手的实现
"这是编程手实现的代码，帮我对照我的模型设计，检查实现是否正确"
```

---

## 六、3 轮专题训练 × Claude Code 使用策略

| 单元 | 主题 | Claude Code 重点用法 |
|------|------|---------------------|
| **单元一** | 预测与评价类 | 调研预测模型库（回归/时间序列/灰色/神经网络）；对比评价方法（AHP/TOPSIS/熵权/模糊） |
| **单元二** | 优化与仿真类 | 调研优化算法（遗传/粒子群/模拟退火）；分析约束条件建模方式 |
| **单元三** | 全真模拟 | 综合运用所有储备，快速判断题型 → 调出历史调研结论 → 确定框架 |

---

## 七、4 天实战节奏 × Claude Code 使用策略

| 天数 | 你的任务 | Claude Code 用法 |
|------|----------|------------------|
| **Day1** | 审题 + 确定建模框架 | `brainstorming` 深度讨论，产出结构化建模方案 |
| **Day2** | 公式推导 + 指导编程手 | 推导验证；输出清晰的公式文档给编程手 |
| **Day3** | 验证模型合理性 | 检查编程手的结果是否合理；跟原始假设对比 |
| **Day4** | 配合论文手写模型部分 | 自动生成模型描述段落、符号表、流程图 |

---

## 八、国赛 72 小时的 Claude Code 策略

| 时间段 | 重点 | 用法 |
|--------|------|------|
| 前 4 小时 | 选题决策 | 对每个候选题目快速 brainstorm，评估建模可行性和团队能力匹配度 |
| 4-12 小时 | 建模框架 | 确定方案，输出结构化文档给编程手开始编码 |
| 12-36 小时 | 模型细化 + 验证 | 边看编程手的结果边调整模型细节 |
| 36-54 小时 | 论文模型部分 | 输出模型描述、符号表、流程图给论文手 |
| 54-72 小时 | 查漏补缺 | 检查模型假设是否充分论证、创新点是否突出 |

---

> 📌 **一句话**：你的价值在于**模型选型判断**和**问题抽象能力** — Claude Code 帮你穷举方案、验证逻辑、整理文档，但"这道题该用什么模型"的最终决策在你。
