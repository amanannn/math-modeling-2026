笔记用途：第一阶段考核 — 基础模型案例汇总（优化类）
侧重：编程实现视角，配 scipy/pulp 代码示例
日期：2026-07-12

1 线性规划 (Linear Programming, LP)

适用场景：目标函数和约束条件都是线性的

标准形式：

经典案例 — 生产计划问题：

某工厂生产 A、B 两种产品。
A 利润 40 元/件，B 利润 60 元/件。
A 需 1h 机器加工 + 2h 人工，B 需 2h 机器加工 + 1h 人工。
每天可用机器 8h、人工 10h。
问如何安排产量使利润最大？

建模：

Python 求解（scipy）：

国赛常考变体：运输问题、指派问题、资源分配、下料问题、投资组合

2 非线性规划 (Nonlinear Programming, NLP)

适用场景：目标函数或约束中有非线性项

经典案例 — 选址问题：

在平面上建一座仓库，服务 4 个客户点：(0,0)、(4,0)、(0,3)、(4,3)。
求仓库位置使得到各客户的距离平方和最小。

建模：

Python 求解：

scipy.optimize 常用求解器：

国赛常考变体：参数拟合（最小二乘）、最优控制、结构优化设计、反应动力学参数估计

3 二次规划 (Quadratic Programming, QP)

适用场景：二次目标 + 线性约束 — 投资组合优化、SVM、模型预测控制

经典案例 — Markowitz 投资组合：

Python 求解：

4 凸优化

核心优势：局部最优 = 全局最优，求解高效稳定

判断凸性：
目标函数 f(x) 是凸函数（Hessian 半正定）
约束函数 gi(x) 是凸函数（约束集是凸集）

国赛应用：很多问题可通过变形转化为凸优化 → 保证全局最优解
最小二乘拟合 → 凸
LASSO/岭回归 → 凸
最大熵模型 → 凸
一些几何问题（如最小覆盖圆）→ 凸

1 整数规划 (Integer Programming, IP)

适用场景：变量必须取整数（人数、车辆数、设备台数）

核心思想：

经典案例 — 下料问题：

有 7m 长的钢管，需截取 2.5m 的 100 根和 1.6m 的 150 根。问最少需要多少根 7m 钢管？

Python 求解（pulp）：

国赛常考变体：下料问题、背包问题、选址问题、车辆路径问题 (VRP)

2 0-1 整数规划 (Binary Integer Programming, BIP)

适用场景：决策变量只能取 0 或 1（选/不选、开/关、建/不建）

经典案例 — 投资项目选择：

公司有 500 万资金，4 个候选项目。各项目投资额和预期收益如下，问选哪些？
| 项目 | 投资(万) | 收益(万) |
|------|---------|---------|
| 1 | 200 | 80 |
| 2 | 150 | 60 |
| 3 | 180 | 70 |
| 4 | 100 | 45 |

建模：

Python 求解：

国赛常考变体：选址-分配问题、集合覆盖、背包问题、设施选址、排班问题

3 混合整数规划 (Mixed Integer Programming, MIP)

适用场景：部分变量为整数、部分为连续 — 国赛中最常见

经典案例 — 带固定成本的选址-运输问题：

在 m 个候选地点中选部分建仓库（有固定成本），服务 n 个客户。每个仓库有容量限制。求选址+运输的最优方案。

建模：

四、求解器选型速查表

国赛推荐组合：
简单LP/NLP → SciPy（无需额外安装）
整数规划/MIP → PuLP + CBC（开源免费，安装：pip install pulp）
凸优化/复杂QP → CVXPY（pip install cvxpy）
大规模/竞赛冲刺 → Gurobi 学术免费版（效率最高）

七、编程手速查代码模板

通用 LP 模板

通用 MIP 模板

本笔记覆盖了数模中最常用的优化方法。
线性/非线性 + 整数规划是国赛高频考点，配合 SciPy + PuLP 两个库基本能解决 80% 的优化类赛题。
建议每个模型类型都跑通一个 demo，第二阶段训练时直接套模板。