# 数学建模国赛 — 编程手工作区

## 项目背景
2026 年高教社杯全国大学生数学建模竞赛备赛。72 小时竞赛（9.10 18:00 - 9.13 20:00）。

培训分两阶段：双创周基础夯实（7.1-7.19）+ 暑期线下专项实训（8.12-9.6，3 轮专题训练）。

详见 `2026数模国赛培训计划_队长分析总结.md`。

## 我的角色
编程手兼队长。负责：代码实现、数据处理、算法调试、可视化、团队管理、代码仓库维护。

队友：建模手（模型设计与公式推导）、论文手（论文撰写与排版）。

## 编程环境
- **主语言**：Python 3.12 (NumPy, SciPy, Pandas, Matplotlib, Scikit-learn)
- **辅助**：MATLAB（按需）
- **排版**：LaTeX（配合论文手编译验证）
- **版本控制**：Git
- **Shell**：Git Bash (MinGW)

## 项目结构
```
D:\虚拟C盘\数学建模培训\
├── models/         — 各专题模型代码（预测/评价/优化/仿真）
├── data/           — 数据集（训练用 + 国赛用）
├── utils/          — 通用工具函数（数据读写、可视化、统计）
├── templates/      — 代码模板（模型模板、图表模板、数据预处理模板）
├── paper/          — 论文相关（LaTeX 源码、图表输出）
├── notes/          — 学习笔记、软件实操截图（第一阶段考核用）
├── output/         — 图表、结果导出
└── CLAUDE.md       — 本文件
```

## 代码规范
- 每个 `.py` 文件顶部注明：用途、输入、输出、调用示例
- 函数添加中文注释说明用途和参数含义
- 每个模型提供独立的 `demo_*.py` 演示脚本
- 图表导出为高清 PNG（300dpi）+ EPS 矢量图（论文手可直接用）
- 使用 Git 管理所有代码：每次训练后打 tag（如 `unit1-submit`, `unit2-submit`）
- 模块间用相对路径引用，保持项目可移植

## 三个训练单元的代码重点
1. **单元一（预测与评价类）**：回归、时间序列、灰色预测、神经网络 / AHP、TOPSIS、熵权法、模糊综合评价
2. **单元二（优化与仿真类）**：线性/非线性规划、遗传算法、粒子群、模拟退火 / 蒙特卡洛、元胞自动机
3. **单元三（综合创新类）**：全题型综合，侧重快速选型 + 创新算法设计

## 常用指令
- 写代码前先 brainstorm 算法方案：对比至少 2 种候选方案再动手
- 实现模型后立即写测试验证（TDD 模式）：构造已知答案的简单用例
- 出图时确认：中文字体、坐标轴标签、单位、图例、导出格式
- 提交代码前运行 `/code-review` 检查质量
- 每次训练结束后用 Git 打 tag，整理代码到对应模型目录
